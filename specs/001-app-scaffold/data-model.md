# Data Model
**Feature**: Basic Application Structure

There are no strict database requirements at this scaffolding stage. PostgreSQL integration is marked as optional.

## Initial Setup
- **PostgreSQL**: A basic connection driver (e.g. `pg`) will be included in the backend `package.json`, but the database does not need tables for this feature.
- Future features (such as Meeting Rooms details) will define explicit entities and relations.
